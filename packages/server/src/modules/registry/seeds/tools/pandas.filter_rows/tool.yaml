name: pandas.filter_rows
version: 1
description: Filter rows of a DataFrame using a query expression.
category: data_transformation
inputs:
  df:
    schema: DataFrame
    required: true
    description: Input DataFrame to filter.
  query:
    schema: String
    required: true
    description: Query string expression (passed to DataFrame.query()).
outputs:
  filtered:
    schema: DataFrame
    description: Filtered DataFrame.
implementation:
  language: python
  entry_point: tool.py:run
